# 交接文档（第十六批）：中文平台真实验证 —— B站 / 无口播 / 文字兜底（2026-07）

> 庖丁主打「B站/抖音/小红书」中文平台，但至今全部真实验证都在 YouTube。本批补上产品核心路径的真机验证。
> 发现代码 bug：修复+测试+单独 commit。**绝对不要 git push**。额度红线 weekly 60%。

## P1：B站路径（含反爬 cookies）

1. 用 `yt-dlp "bilisearch5:家常菜 教程"` 搜 1–5 分钟短视频，选一条跑全链路（--images）。
2. 若遇 HTTP 412 反爬：`.env` 设 `PAODING_COOKIES_FROM_BROWSER=chrome` 复跑（本机 Chrome 有登录态）；记录该路径是否工作、错误信息是否对用户友好（412 时有没有提示用户配 cookies？没有就加个友好提示，这是真实用户第一个会撞的墙）。
3. 验收：标题/简介进菜谱、ASR 中文质量、source_time、配图正常。

## P2：无口播/纯字幕视频（VL 读屏兜底主场景）

B站/YouTube 搜一条**没有口播、只有画面字幕**的做菜视频（常见：纯 BGM+字幕流派，搜"做菜 无人声 字幕"或从 P1 搜索结果里挑）。验收：ASR 近乎空 → 视觉读屏文本撑起结构化 → 菜谱有效（食材/步骤完整度如实记录）。这是「读画面字幕」功能的真实主场景，从未真测过。

## P3：小红书/图文兜底路径

1. 找一个小红书菜谱帖链接（网页搜索或已知格式 xiaohongshu.com/explore/...），走 parse-url：预期 yt-dlp 无抽取器 → 自动兜底抓网页文字 → 文字管线出菜谱。记录兜底是否顺畅、反爬表现、错误提示友好度。
2. 拿不到小红书就用任意公众号/菜谱网页文章验证同一兜底路径。

## P4：报告更新 + 收尾

1. 三条路径结果补进 docs/verification-2026-07.md（新章节「中文平台验证」，✅/⚠️/❌ 如实）。
2. 遇到的每个代码级问题修复+测试+commit；平台级限制（反爬无解等）如实记录。
3. 最终 `npm test` 全绿、工作区干净、未 push、中文总结（含额度余量）。
