# 交接文档（第七批·收官）：对抗性全量审查 + 实用小功能（2026-07）

> 前置：前六批共 33 个提交、89/89 测试全绿。本批是收官批。
> 按 G1 → G4 顺序，**每项 `npm test` 全绿后单独 commit**（gpg 用 `-c commit.gpgsign=false`）。**绝对不要 git push**。

## 项目约束（不变）

零第三方运行时依赖；`node --test`；简体中文；README/.env.example 同步；诚实原则。

---

## G1：对抗性全量审查（本批核心，花最多时间）

你是一个全新会话，没有之前实现时的思维定势——用**挑刺者/攻击者视角**重审 `git diff origin/main..HEAD` 的全部改动（30+ 文件），逐文件通读。这不是走过场：前六批全是 AI 写的、只做过一轮自查（C5/D4/E4/F4 各自批内），跨批交互从未被整体审过。

重点攻击面：
1. **安全**：新增的十几个端点（jobs/techniques/import-recipe/parse-images/backups/summary…）逐个核对鉴权+限流是否真的生效（写个临时脚本枚举路由 vs 豁免清单）；multi-user（D3）token 解析的常量时间语义；文件名/路径注入（备份名、技法缓存名、用户名、上传文件名）；分享页 `/r/` 的零写入面；SSRF 过滤有没有被新代码路径绕过（parse-images？import-recipe 的字段？）。
2. **资源泄漏**：B1 队列在任务抛异常/超时/服务关闭时槽位与落盘状态是否一致；B3 流式上传中断（客户端断开）时临时文件与预占槽清理；E3 备份定时器 unref；F1 图片上传的临时文件。
3. **数据一致性**：E2 列表缓存的失效路径 vs 所有写入点（保存/删除/导入/解析完成/图片导入/AI 补讲解写回/技法缓存）逐一对照；B4 合并函数丢数据边界（两边同时改同一菜谱 meta）；D2 重排后 nutrition/图片/source_time 是否真的跟随。
4. **跨批交互**：D3 多用户与 B4 rev、B1 最近任务视图、E3 备份（备份是否包含所有用户文件）、F1 新任务类型与任务持久化的兼容。
5. **前端**：跟做模式/编辑器在菜谱缺字段（老数据、imported 菜、无营养）时的空指针；C2 筛选与 F1 图片菜的交互。

**产出**：每发现一个真实问题：修复 + 对应测试 + 单独 commit（`fix: 审查修复 —— <问题>`）。没有问题的检查面在总结里列出"查了什么、为什么没问题"。宁可多报告少改动——不确定是否 bug 的，列入总结由人裁决，不要凭猜测改行为。

## G2：打印/PDF 友好

竞品（Flavorish/Paprika）标配。菜谱详情页加「打印」按钮（`window.print()`）+ `@media print` 样式：隐藏导航/按钮/底栏，食材+步骤+why 排版成单栏文档，图片限宽，页眉带标题与来源 URL。纯 CSS + 一个按钮，不引库。

## G3：中餐单位速查

份量缩放已有 qty/unit。加一个轻量单位换算提示：食材行的"勺/克/两/毫升"可点开小气泡显示常用换算（1两=50g、1瓷勺≈15ml 等，静态数据表）。换算表纯数据 + 查询纯函数配测试。放 `app.js` 可测试区。

## G4：收官

1. README 最终校对：功能表与实际实现一致（逐条核对）、Roadmap 状态准确、快速开始三方案（本地/云/Docker）可照抄执行。
2. 全分支最终 `npm test`；`git diff --check`；确认未 push。
3. 输出**总结报告**：六批+本批的完整清单、G1 审查结论（修了什么/查了什么没问题/遗留给人裁决的疑点）、建议人工验证项（Docker build、Android 打包、真实视频回归）。

---

## 验收清单

- [ ] G1 每个修复独立 commit 且带测试；不确定的疑点写进总结而非擅自改
- [ ] G2/G3 独立 commit
- [ ] 最终全绿、工作区干净、未 push
